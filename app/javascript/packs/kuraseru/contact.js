document.addEventListener('DOMContentLoaded', () => {

  const submit = document.querySelector('送信');
  console.log(submit);
  

  submit.addEventListener('click', () => {
    console.log(123);
  })
});